{
  "name": "ott-platform-functions",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.7"
  },
  "engines": {
    "node": "18"
  }
  ,
  "scripts": {
    "test": "mocha --exit test/**/*.test.js",
    "emulator:test": "firebase emulators:exec --only firestore,functions \"mocha --exit test/**/*.test.js\""
  }
}
